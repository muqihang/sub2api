# 逐梦服务特定条款

**生效及最后更新日期：2026 年 7 月 23 日**

本条款补充《逐梦服务条款》。如有冲突，本服务特定条款就相关功能优先适用，同时不得减轻上游供应商规则。

## 1. API、模型与路由

1. 模型标识可映射到不同供应商、版本、账户池或兼容接口。除非订单明确承诺，您无权要求固定供应商、账户、数据中心或版本。
2. 我们可为容量、安全、价格、质量或故障恢复执行负载均衡、重试、降级和故障转移。不同路由的输出、延迟、计量和工具能力可能不同。
3. 您应自行实施幂等、超时、退避、重试上限和重复输出处理。自动重试可能产生额外计费；不得把 HTTP 状态码视为唯一结算依据。

## 2. Responses、Chat、流式与 Compact

1. 兼容接口不表示与任何官方 SDK 或服务完全一致。字段、事件顺序、终止原因、加密内容、思考块、工具块和错误格式可能被规范化或透传。
2. 流式连接可因客户端网络、CDN、代理、上游超时或路由切换提前中断。您必须保存必要状态，并避免把未验证的部分输出当作完整结果。
3. Compact、上下文压缩和远程摘要可能调用模型并产生用量。压缩结果可能遗漏、概括或改变细节；关键指令、凭据和事实应由您独立保存与校验。

## 3. 搜索和工具调用

1. Web Search、文件检索、代码执行和其他工具可能由上游或第三方提供，具有独立条款、区域限制、配额和内容许可。
2. 搜索结果、摘要、链接和引用可能过时、不完整或错误。您必须核验来源，不得利用服务大规模抓取、建立未经授权的索引或数据库、绕过站点访问限制，或侵犯内容权利。
3. 工具调用可能访问外部资源或执行具有副作用的操作。您必须设置权限边界、确认步骤和人工监督；逐梦不对您授权的第三方操作负责。

## 4. API Key、并发与调度

1. API Key 仅供获授权的账户和应用使用。并发、速率和模型权限以平台实时配置为准，可按账户、Key、分组、模型、IP 或风险级别执行。
2. 高并发、异常请求、长连接占用、规避限速或影响共享容量时，我们可排队、拒绝、降低优先级或暂停调度。
3. “可用”“正常”或监控通过仅表示特定时间的检查结果，不保证后续请求成功。上游额度、周限额、工作区状态和授权可随时变化。

## 5. 数据、日志与保留

1. 为路由、计量、排障、安全和争议处理，我们可记录请求标识、模型、令牌数、状态、错误、延迟、IP、Key 标识和必要的有限内容。具体保留受运营、安全及法律需要影响。
2. 不应提交无需处理的秘密、受监管数据或特别敏感个人信息。必须提交时，您负责取得授权、最小化数据、加密并选择适当模型和地区。
3. 第三方和上游的数据处理由其条款约束。逐梦不能代表上游承诺零保留、训练排除、数据驻留或删除时限，除非我们书面明确确认。

## 6. 支持、维护和测试功能

1. 支持按商业上合理努力提供，不承诺固定响应或修复时间，除非另有书面 SLA。
2. Beta、实验、预览、别名和新接入功能可能无通知变更或撤回，不适合关键生产负载。
3. 我们可安排维护、限制异常流量或紧急停用有风险的路由。状态页、监控和客服信息仅供参考，不构成持续可用承诺。

联系邮箱：`admin@5566676.xyz`
