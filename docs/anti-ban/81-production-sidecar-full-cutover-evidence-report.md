# Plan81 Production Sidecar Full Cutover Evidence Report

## Final decision

**BLOCKED_PRODUCTION_SIDECAR_CONFIG_VALIDATION**

Production sidecar full cutover was **not** executed. The dry validation gate failed because the currently deployed Go/uTLS sidecar binary still requires an explicit loopback test dial override. That conflicts with the Plan81 production gate requiring dial override disabled and real `api.anthropic.com:443` selected only by server policy.

## Production baseline

- Server: `198.12.67.185`
- Baseline time bucket: `2026-07-03T07:24Z`
- Current production entry: `18080`
- Current production state before validation:
  - `18080`: new Plan79B Sub2API host process, healthy
  - `18443`: new CC Gateway host process on loopback
  - `18445`: current egress proxy on loopback
  - `18081`: old service still running, untouched
  - `3012/3017`: no listener observed
- Old Docker rollback pair: present and stopped, not deleted
- DB/Redis health bucket: PostgreSQL container healthy; Redis container healthy

## Public access / root 404 addendum

The public `http://198.12.67.185:18080/` issue was resolved before Plan81 validation.

Root cause:
- Docker-published ports previously installed Docker NAT/proxy rules.
- Plan79B uses a host Sub2API process, so UFW needed an explicit `18080/tcp` allow rule.
- After firewall was fixed, `/health` was reachable but `/` returned 404 because the first Plan79B Sub2API binary was backend-only and built without embedded frontend assets.

Fix performed:
- Built a latest Plan79B Sub2API linux/amd64 binary with embedded frontend assets (`-tags embed`).
- Replaced only the Plan79B-owned `18080` Sub2API host process with the embedded-frontend binary.
- Did not touch `18081`, `3012`, or `3017`.
- Did not delete the old Docker pair.

Safe artifact bucket:
- Embedded Sub2API binary sha256: `1f0dbc77027fe51f47db535d09af5cd4c0202914353d5c2469c17bb173f68e98`

Verification:
- Internal `http://127.0.0.1:18080/health`: `200`
- Internal `http://127.0.0.1:18080/`: `200`, `text/html`
- Public `http://198.12.67.185:18080/health`: `200`
- Public `http://198.12.67.185:18080/`: `200`, `text/html`

## Production sidecar config validation gate

Required gate status:

| Gate | Result | Evidence bucket |
| --- | --- | --- |
| `egress_tls_sidecar.enabled=true` candidate config | Not applied | Current production config remains sidecar disabled |
| `mock_messages_response.enabled=false` | PASS | Current config shows disabled |
| `dial_override disabled` | FAIL | Sidecar binary requires explicit loopback test dial override |
| allowed upstream host exactly `api.anthropic.com:443` | BLOCKED | Cannot validate production sidecar mode while dial override is mandatory |
| target_host/SNI/Host server-selected only | BLOCKED | Not cut over |
| profile ref server-selected only | BLOCKED | Not cut over |
| 2.1.197 TLS profile available | Present in allowlist bucket | Not activated |
| fallback/rollback 2.1.179 TLS profile available | Present in allowlist bucket | Not activated |
| Node direct HTTPS fallback disabled/fail-closed | Current non-sidecar production path unchanged | Production sidecar path not activated |
| sidecar health PASS | BLOCKED | Production-mode sidecar does not start without test override |
| CC Gateway config load PASS | Current production config load PASS | Sidecar-enabled production config not activated |
| no provider-direct path except through sidecar | BLOCKED | Not cut over |
| rollback path prepared | PASS | Plan79B host-process rollback and old Docker pair remain available |

Dry validation command used a sanitized environment with no mock collector and no dial override. Result:

- Validation status: `exited:1`
- Stable error bucket: `test dial override must be explicit loopback`

This is a hard blocker for production sidecar cutover because enabling the currently required override would violate Plan81's absolute prohibitions:

- no loopback test dial override for production;
- no production traffic to mock collector;
- no hard cut when validation fails.

## Cutover result

- Full sidecar egress cutover: **not executed**
- Mock bridge: **not enabled**
- Loopback test dial override: **not enabled**
- Production traffic to mock collector: **none**
- Extra Claude account canary request: **not sent**
- Real upstream request intentionally generated by Plan81: **0**

## Health after blocked validation

- `18080` health: PASS
- `18080` root UI: PASS
- `18443` CC Gateway listener: present
- `18445` current egress proxy listener: present
- `18081` old service: still running, untouched
- `3012/3017`: untouched
- DB/Redis health bucket: healthy

## Error bucket summary

- Public root 404: fixed by embedded frontend binary.
- Production sidecar validation: blocked by current sidecar binary's test-only dial override requirement.
- No raw prompt/body/response, secrets, cookie, account id, DB URL, Redis URL, or proxy credential recorded.

## Rollback availability

Rollback remains available:

1. Stop the current Plan79B host Sub2API process if required.
2. Restore previous Plan79B non-sidecar config, or restart the old Docker pair from the preserved rollback path.
3. Verify `18080/health`.
4. Do not restore DB/Redis unless a separate data anomaly is proven and manually approved.

## Final current production state

- `18080`: latest Plan79B Sub2API host process with embedded frontend, health PASS, public root PASS.
- CC Gateway: latest Plan79B CC Gateway on `127.0.0.1:18443`.
- Production sidecar: **not cut over**, because validation failed.
- Mock bridge: disabled.
- Dial override: disabled for production; not used.
- Live canary: not performed.
- Separate Claude account canary: skipped per user instruction.
